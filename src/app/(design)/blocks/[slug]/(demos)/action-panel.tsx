import { ActionPanel } from "@/components/ui/action-panel";

export const actionPanelBlock = {
  name: "action-panel",
  components: {
    Default: <ActionPanel>Action Panel Block Content</ActionPanel>,
  },
};
